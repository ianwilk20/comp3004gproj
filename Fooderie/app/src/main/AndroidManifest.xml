<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fooderie">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".OptionsActivity"
            android:label="@string/title_activity_options"></activity>
        <activity android:name="fooderie.recipeBrowser.rbWebsite" />
        <activity android:name="fooderie.recipeBrowser.rbDisplay" />
        <activity android:name="fooderie.recipeBrowser.rbSearch" />
        <activity android:name="fooderie.mealPlanner.views.PlanRecipeRecyclerView" />
        <activity android:name="fooderie.mealPlannerScheduler.views.WeeklyScheduleFragment_Proxy" />
        <activity android:name="fooderie.groceryList.views.GroceryListView" />
        <activity android:name="fooderie.CookingAssistant.views.views.CookingAssistantViewer" />
        <activity android:name="fooderie.CookingAssistant.views.views.CookingAssistantPreview" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>